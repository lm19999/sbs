#jdbc连接
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://39.105.143.125:3306/sbs?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false&useSSL=false&testOnBorrow=true&allowMultiQueries=true
    username: root
    password: sjw971770759
    druid:
      filters: stat
      max-active: 20
      initial-size: 1
      max-wait: 60000
      min-idle: 1
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: select 1
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 1000MB
  jpa:
    show-sql: true
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
  http:
    multipart:
      max-file-size: 10Mb
      max-request-size: 10Mb
  redis:
    host: 39.105.143.125
    database: 1
    port: 7001
    password: redisTest
    timeout: 2000
    lettuce:
      pool:
        max-wait: 6000

#驼峰命名转换
mybatis:
  configuration:
    mapUnderscoreToCamelCase: true

#logging
logging:
  level:
    com.systop.sbs: debug
